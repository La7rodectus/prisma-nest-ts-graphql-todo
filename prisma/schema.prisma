generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("DB_URL")
}

model Note {
  id Int @id @default(autoincrement())
  name String
  description String
  comments_list_id Int
}


